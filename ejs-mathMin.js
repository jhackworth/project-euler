// Eloquent JavaScript
// Write a function that takes two arguments and returns their minimum

function min(x,y){
  return Math.min(x,y);
}

console.log(min(0, 10));
// → 0
console.log(min(0, -10));
// → -10